// Re-export all shared components for simpler imports
// Allows: import { Card, Button } from '../components'
// Or: import Card from '../components/Card' via individual files

export * from './shared';
